<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Upload PYQ</title>
  <link rel="stylesheet" href="/css/pyq.css">
</head>
<body>
  <header>
    <h1>Upload Previous Year Question Paper</h1>
  </header>

  <% if (user && user.role === 'admin') { %>
    <div class="container">
      <form action="/pyq/upload" method="POST" enctype="multipart/form-data">
        <div class="form-group">
          <label for="branch">Select Branch</label>
          <select name="branch" id="branch" required>
            <option value="CSE">CSE</option>
            <option value="AI">AI</option>
            <option value="ECE">ECE</option>
            <option value="EEE">EEE</option>
            <option value="IoT">IoT</option>
            <option value="Mechanical">Mechanical Engineering</option>
            <option value="Civil">Civil Engineering</option>
          </select>
        </div>

        <div class="form-group">
          <label for="semester">Select Semester</label>
          <select name="semester" id="semester" required>
            <% for (let i = 1; i <= 8; i++) { %>
              <option value="<%= i %>">Semester <%= i %></option>
            <% } %>
          </select>
        </div>

        <div class="form-group">
          <label for="file">Upload Question Paper (PDF or DOCX)</label>
          <input type="file" name="pdfFile" id="file" accept=".pdf, .docx" required />
        </div>

        <button type="submit">Upload</button>
      </form>
    </div>
  <% } else { %>
    <p style="text-align: center; margin-top: 2rem; color: red;">
      Access denied. Only admins can upload question papers.
    </p>
  <% } %>

  <footer>
    <p>© 2025 PYQ Portal. All rights reserved.</p>
  </footer>
</body>
</html>

